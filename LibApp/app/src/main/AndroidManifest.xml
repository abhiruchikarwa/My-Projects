<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="projtest.ak.com.libapp" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        >

        <activity
            android:name=".Splash"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Main"
            android:label="@string/title_activity_main" >
        </activity>

        <activity
            android:name=".Update"
            android:label="@string/title_activity_update" >
        </activity>

        <activity
            android:name=".UAdd"
            android:label="@string/title_activity_uadd">
        </activity>

        <activity
            android:name=".URemove"
            android:label="@string/title_activity_uremove" >
        </activity>

        <activity
            android:name=".UView"
            android:label="@string/title_activity_uview" >
        </activity>

        <activity
            android:name=".Manage"
            android:label="@string/title_activity_manage" >
        </activity>

        <activity
            android:name=".MAdd"
            android:label="@string/title_activity_madd" >
        </activity>

        <activity
            android:name=".MRemove"
            android:label="@string/title_activity_mremove" >
        </activity>

    </application>

</manifest>
